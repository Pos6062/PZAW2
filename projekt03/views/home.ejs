<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <script src="/populate.js"></script>

  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: Arial, Helvetica, sans-serif;
    }

    body {
      min-height: 100vh;
      background: #f4f6f8;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    main {
      background: #ffffff;
      width: 100%;
      max-width: 600px;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    }

    h1 {
      text-align: center;
      margin-bottom: 1.5rem;
      color: #333;
    }

    .top-link {
      display: block;
      text-align: center;
      margin-bottom: 1rem;
      font-weight: bold;
      color: #0066cc;
      text-decoration: none;
    }

    .top-link:hover {
      text-decoration: underline;
    }

    ul {
      list-style: none;
      margin-top: 1rem;
    }

    li {
      padding: 0.75rem 1rem;
      margin-bottom: 0.5rem;
      background: #f1f3f5;
      border-radius: 6px;
      display: flex;
      gap: 0.75rem;
      font-weight: 500;
    }

    .actions {
      margin-top: 1.5rem;
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .btn {
      display: block;
      text-align: center;
      padding: 0.7rem;
      border-radius: 6px;
      font-weight: bold;
      text-decoration: none;
      color: white;
      background: #0066cc;
      transition: background 0.2s ease;
    }

    .btn:hover {
      background: #0052a3;
    }

    .btn.secondary {
      background: #6c757d;
    }

    .btn.secondary:hover {
      background: #5a6268;
    }
  </style>
</head>

<body>
  <main>
    <h1><%= title %></h1>

    <a href="/about" class="top-link">About us</a>

    <ul id="people">
      <% people.forEach(function(person){ %>
        <li>
          <span><%= person.id %></span>
          <span><%= person.fname %></span>
          <span><%= person.lname %></span>
        </li>
      <% }) %>
    </ul>

    <div class="actions">
      <a href="/edit" class="btn secondary">Edit</a>
      <a href="/form" class="btn">Sign in NOW</a>
    </div>
  </main>
</body>
</html>