<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="/populate.js"></script>
  </head>
  <body>
    <main>
      <h1><%= title %></h1>
      <a href="/about">about us</a>
        <ul id="people">
        <% people.forEach(function(person){ %>
            <li><%= person.id %> <%= person.fname %> <%= person.lname %></li>
            <!-- <button onclick="del('<%= person.id %>')">usuń</button>
            <button onclick="updatePerson('<%= person.id %>','<%= person.fname %>','<%= person.lname %>')">Edytuj</button> -->
        <% }) %>
        <a href="/edit">edit</a>
        <a href="/form">Sign in NOW</a>
        <button onclick="addPeople()">add people</button>
    </ul>
    </main>
  </body>
</html>